# Build outputs
bin/
build/
target/
node_modules/
prebuilds/
*.node
/tree-sitter-xquery.wasm
# Rebuilt fresh by .github/workflows/pages.yml on every deploy; the rest of
# docs/ (playground shell, web-tree-sitter runtime) is checked in and static.
/docs/tree-sitter-xquery.wasm

# npm (this repo uses yarn)
package-lock.json

# Scratch
temp/
tmp/
*.log
log.html
.DS_Store

# Local tooling — not part of the grammar
.claude/
.moderne/
.zed/
.codacy/
.cursor/
